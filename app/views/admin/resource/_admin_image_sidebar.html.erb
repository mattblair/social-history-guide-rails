<%-# hard code link during development, might use pipeline for server %>

<% if !resource.image_name.nil? %>
<img alt="Current Image" class="admin_sidebar_image" width="240" height="180" src=<%="#{ENV['KYC_STATIC_PHOTOS_URL']}#{resource.image_name}.jpg" %> />
<% else %>
<img alt="Placeholder" class="admin_sidebar_image" width="240" height="180" src=<%="#{ENV['KYC_STATIC_PHOTOS_URL']}kyc-placeholder.jpg" %> />
<% end %>